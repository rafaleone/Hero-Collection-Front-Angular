// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // backEnd_APIUrl: 'https://jxtltcvvpb.execute-api.sa-east-1.amazonaws.com/Prod/api',  
  backEnd_APIUrl: 'http://localhost:61511/api',
  moralis_serverUrl: "https://xr5zz8eswexv.usemoralis.com:2053/server",
  moralis_appId: "iIKLj6urW4nghHrMFcqjxxlfacH27mXItWE4mOj1"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
